using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class canvashelper : MonoBehaviour
{
    public GameObject canvsd;
    // Start is called before the first frame update
    void Start()
    {
        // Canvas.
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
